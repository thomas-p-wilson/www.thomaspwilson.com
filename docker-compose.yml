version: '3.6'

services:
  node:
    image: thomaspwilson-dev
    build:
      context: .
      dockerfile: .docker/Dockerfile
    volumes:
      - ./:/usr/src/app
    working_dir: /usr/src/app
    ports:
      - 8000:8000